import { FilterphonePipe } from './filterphone.pipe';

describe('FilterphonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterphonePipe();
    expect(pipe).toBeTruthy();
  });
});
